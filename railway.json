{
    "build": {
      "env": {
        "PYTHON_VERSION": "3.10"
      },
      "commands": [
        "pip install -r requirements.txt"
      ]
    },
    "deploy": {
      "startCommand": "uvicorn main:main --host 0.0.0.0 --port $PORT"
    }
  }
  
